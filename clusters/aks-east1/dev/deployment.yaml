apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-app-dev
  labels:
    app: my-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: my-app
  template:
    metadata:
      labels:
        app: my-app
    spec:
      containers:
        - name: my-app
          image: nginx:1.23
          ports:
            - containerPort: 80
